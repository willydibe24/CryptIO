@AGENTS.md

## Claude Code

No Claude-specific notes yet beyond what's imported from `AGENTS.md`. Add
here only instructions that apply *exclusively* to Claude Code (e.g. when
to use plan mode, commands to never run) — everything else belongs in
`AGENTS.md`, which is the shared source of truth for other coding agents
too.
